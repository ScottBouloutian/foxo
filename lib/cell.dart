import 'package:flutter/material.dart';

class Cell extends StatelessWidget {
    @override Widget build(BuildContext context) {
        return new Image(image: AssetImage('images/cell.png'));
    }
}
